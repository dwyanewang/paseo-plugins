# Installed dependencies. Both plugins link the Paseo SDK through `file:../../paseo/packages/*`,
# so node_modules is a set of symlinks into a sibling checkout and is never portable.
node_modules/

# Build and test output
dist/
coverage/
*.tsbuildinfo

# Screenshots, snapshots and console logs from the Playwright MCP server, written to the repo root
.playwright-mcp/

# Editor and OS noise
.DS_Store
*.swp
